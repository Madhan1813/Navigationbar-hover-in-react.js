import React from 'react'
import '../components/Nav.css'

const Nav = () => {
  return (
    
    <div class="bar">
        <a href="#">Home</a>
        <a href="#">About us</a>
        <a href="#">jobs</a>
        <a href="#">login</a>
        <a href="#">service</a>
        <span></span>
    </div>
  )
}

export default Nav